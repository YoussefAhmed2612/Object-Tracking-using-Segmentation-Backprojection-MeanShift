# Object-Tracking-using-Segmentation-Backprojection-MeanShift
Implements object tracking using segmentation, Histogram Backprojection, and MeanShift. The moving object is segmented from the first frame to extract the ROI, then tracked across frames. The video is displayed with a dynamic bounding box. All logic is modular within a single Python file.
